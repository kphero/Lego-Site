<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/css/main.css" />

    <title>Lego Collection</title>
</head>
<body>
    <header>
      <%- include('partials/navbar', {page:'/login'}) %>
    </header>
    <main class="container mx-auto">
        <!-- Hero splash -->
        <div class="hero min-h-screen bg-base-200">
            <div class="hero-content text-center">
              <div class="max-w-md">
                <h1 class="text-5xl font-bold">Log In</h1>
                <p class="py-6">Please provide your credentials below</p>
              </div>
            </div>
          </div>
          <!-- Form -->
        <form action="/login" method="POST" class="mt-10">
            <div class="form-control">
              <label class="label" for="name">
                <span class="label-text">User Name</span>
              </label>
              <input type="text" id="userName" name="userName" class="input input-bordered" placeholder="User Name" value="<%= typeof userName != 'undefined' ? userName : '' %>" required>
            </div>
            
            <div class="form-control">
              <label class="label" for="password">
                <span class="label-text">Password</span>
              </label>
              <input type="password" id="password" name="password" class="input input-bordered" placeholder="Password" required>
            </div>

            <div class="form-control mt-6">
                <button type="submit" class="btn btn-primary">Login</button>
            </div>
        </form>

        <% if (errorMessage) { %>
            </br>
            <div class="alert alert-error">
                <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                <span><%= errorMessage %></span>
            </div>
        <% } %>
    </main>
</body>
</html>
